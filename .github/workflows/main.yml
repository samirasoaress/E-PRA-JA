## ServiceProvider

### Schema

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `full_name` | string | Yes |  |
| `email` | string | Yes |  |
| `phone` | string | Yes |  |
| `cpf_cnpj` | string | Yes |  |
| `service_category` | `Limpeza Residencial`, `Mecânico`, `Reboque`, `Chaveiro`, `Babás` | Yes |  |
| `experience_years` | number |  |  |
| `description` | string |  |  |
| `city` | string | Yes |  |
| `state` | string | Yes |  |
| `availability` | `tempo_integral`, `meio_periodo`, `finais_de_semana`, `sob_demanda` |  |  |
| `status` | `pendente`, `aprovado`, `rejeitado` |  |  |
| `id` | string |  | Unique record identifier |
| `created_date` | string |  | Record creation timestamp |
| `updated_date` | string |  | Record last update timestamp |
| `created_by_id` | string |  | ID of the user who created the record |

### Endpoints

### `GET /entities/ServiceProvider`
List ServiceProvider records

**Parameters:**
- `q` (query): JSON query filter, e.g. {"status":"active"}
- `limit` (query): Maximum number of records to return
- `skip` (query): Number of records to skip (pagination)
- `sort_by` (query): Field name to sort by. Prefix with '-' for descending order, e.g. -created_date

```javascript
const records = await base44.entities.ServiceProvider.list();
```

### `POST /entities/ServiceProvider`
Create a ServiceProvider record

```javascript
const record = await base44.entities.ServiceProvider.create({
  // your data
});
```

### `DELETE /entities/ServiceProvider`
Delete multiple ServiceProvider records

```javascript
await base44.entities.ServiceProvider.deleteMany({
  // query filter — WARNING: empty {} deletes ALL records
  full_name: "Example full_name"
});
```

### `POST /entities/ServiceProvider/bulk`
Bulk create ServiceProvider records

```javascript
const records = await base44.entities.ServiceProvider.bulkCreate([
  { /* record 1 */ },
  { /* record 2 */ },
]);
```

### `PUT /entities/ServiceProvider/bulk`
Bulk update ServiceProvider records

```javascript
// bulk-update is not available via SDK — use the REST API
```

### `PATCH /entities/ServiceProvider/update-many`
Update many ServiceProvider records by query

```javascript
// update-many is not available via SDK — use the REST API
```

### `GET /entities/ServiceProvider/{ServiceProvider_id}`
Get a ServiceProvider record by ID

**Parameters:**
- `ServiceProvider_id` (path): Record ID

```javascript
const record = await base44.entities.ServiceProvider.get(recordId);
```

### `PUT /entities/ServiceProvider/{ServiceProvider_id}`
Update a ServiceProvider record

**Parameters:**
- `ServiceProvider_id` (path): Record ID

```javascript
const record = await base44.entities.ServiceProvider.update(recordId, {
  // fields to update
});
```

### `DELETE /entities/ServiceProvider/{ServiceProvider_id}`
Delete a ServiceProvider record

**Parameters:**
- `ServiceProvider_id` (path): Record ID

```javascript
await base44.entities.ServiceProvider.delete(recordId);
```

### `PUT /entities/ServiceProvider/{ServiceProvider_id}/restore`
Restore a deleted ServiceProvider record

**Parameters:**
- `ServiceProvider_id` (path): Record ID

```javascript
const record = await base44.entities.ServiceProvider.restore(recordId);
```

## ServiceRequest

### Schema

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `name` | string | Yes |  |
| `phone` | string | Yes |  |
| `email` | string |  |  |
| `service` | `Limpeza Residencial`, `Mecânico`, `Reboque`, `Chaveiro`, `Babás` | Yes |  |
| `urgency` | `urgente`, `hoje`, `agendar` |  |  |
| `address` | string | Yes |  |
| `preferred_date` | string |  |  |
| `preferred_time` | string |  |  |
| `description` | string | Yes |  |
| `gps_location` | string |  |  |
| `status` | `novo`, `em_andamento`, `concluido`, `cancelado` |  |  |
| `id` | string |  | Unique record identifier |
| `created_date` | string |  | Record creation timestamp |
| `updated_date` | string |  | Record last update timestamp |
| `created_by_id` | string |  | ID of the user who created the record |

### Endpoints

### `GET /entities/ServiceRequest`
List ServiceRequest records

**Parameters:**
- `q` (query): JSON query filter, e.g. {"status":"active"}
- `limit` (query): Maximum number of records to return
- `skip` (query): Number of records to skip (pagination)
- `sort_by` (query): Field name to sort by. Prefix with '-' for descending order, e.g. -created_date

```javascript
const records = await base44.entities.ServiceRequest.list();
```

### `POST /entities/ServiceRequest`
Create a ServiceRequest record

```javascript
const record = await base44.entities.ServiceRequest.create({
  // your data
});
```

### `DELETE /entities/ServiceRequest`
Delete multiple ServiceRequest records

```javascript
await base44.entities.ServiceRequest.deleteMany({
  // query filter — WARNING: empty {} deletes ALL records
  name: "Example name"
});
```

### `POST /entities/ServiceRequest/bulk`
Bulk create ServiceRequest records

```javascript
const records = await base44.entities.ServiceRequest.bulkCreate([
  { /* record 1 */ },
  { /* record 2 */ },
]);
```

### `PUT /entities/ServiceRequest/bulk`
Bulk update ServiceRequest records

```javascript
// bulk-update is not available via SDK — use the REST API
```

### `PATCH /entities/ServiceRequest/update-many`
Update many ServiceRequest records by query

```javascript
// update-many is not available via SDK — use the REST API
```

### `GET /entities/ServiceRequest/{ServiceRequest_id}`
Get a ServiceRequest record by ID

**Parameters:**
- `ServiceRequest_id` (path): Record ID

```javascript
const record = await base44.entities.ServiceRequest.get(recordId);
```

### `PUT /entities/ServiceRequest/{ServiceRequest_id}`
Update a ServiceRequest record

**Parameters:**
- `ServiceRequest_id` (path): Record ID

```javascript
const record = await base44.entities.ServiceRequest.update(recordId, {
  // fields to update
});
```

### `DELETE /entities/ServiceRequest/{ServiceRequest_id}`
Delete a ServiceRequest record

**Parameters:**
- `ServiceRequest_id` (path): Record ID

```javascript
await base44.entities.ServiceRequest.delete(recordId);
```

### `PUT /entities/ServiceRequest/{ServiceRequest_id}/restore`
Restore a deleted ServiceRequest record

**Parameters:**
- `ServiceRequest_id` (path): Record ID

```javascript
const record = await base44.entities.ServiceRequest.restore(recordId);
```

## User

### Schema

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `email` | string | Yes | The email of the user |
| `full_name` | string | Yes | The full name of the user |
| `role` | `admin`, `user` | Yes | The role of the user in the app |
| `id` | string |  | Unique record identifier |
| `created_date` | string |  | Record creation timestamp |
| `updated_date` | string |  | Record last update timestamp |
| `created_by_id` | string |  | ID of the user who created the record |

### Endpoints

### `GET /entities/User`
List User records

**Parameters:**
- `q` (query): JSON query filter, e.g. {"status":"active"}
- `limit` (query): Maximum number of records to return
- `skip` (query): Number of records to skip (pagination)
- `sort_by` (query): Field name to sort by. Prefix with '-' for descending order, e.g. -created_date

```javascript
const records = await base44.entities.User.list();
```

### `POST /entities/User`
Create a User record

```javascript
const record = await base44.entities.User.create({
  // your data
});
```

### `GET /entities/User/{User_id}`
Get a User record by ID

**Parameters:**
- `User_id` (path): Record ID

```javascript
const record = await base44.entities.User.get(recordId);
```

### `PUT /entities/User/{User_id}`
Update a User record

**Parameters:**
- `User_id` (path): Record ID

```javascript
const record = await base44.entities.User.update(recordId, {
  // fields to update
});
```

### `DELETE /entities/User/{User_id}`
Delete a User record

**Parameters:**
- `User_id` (path): Record ID

```javascript
await base44.entities.User.delete(recordId);
```
